#include <iostream>
#include "prime.h"

using std::cout;

int main() {
    cout << "9 is prime? " << is_prime(9) << std::endl;
    cout << "11 is prime? " << is_prime(11) << std::endl;
    cout << "13 is prime? " << is_prime(13) << std::endl;
    cout << "16 is prime? " << is_prime(16) << std::endl;
}
