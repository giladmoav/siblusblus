#include "prime.h"
#include <iostream>

using std::cout;
using std::endl;

int main() {
    cout << "9 is prime? " << is_prime(9) << endl;
    cout << "11 is prime? " << is_prime(11) << endl;
    cout << "13 is prime? " << is_prime(13) << endl;
    cout << "16 is prime? " << is_prime(16) << endl;
}
